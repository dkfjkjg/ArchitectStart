spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://localhost:8769
      fail-fast: true
      enabled: true
      label: main
  profiles:
    active: dev
server:
  port: 8770
